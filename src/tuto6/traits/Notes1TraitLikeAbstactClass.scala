package tuto6.traits

class Notes1TraitLikeAbstactClass(){
  //on peut utiliser des traits à la place de classe abstraite s'il ya des methodes implémentées et on veut que les sous classe
  //implémentent plusieurs traits car les classes ne peut hériter qu'un seul
  //A class can extend only one abstract class, but it can
  //implement multiple traits, so using traits is more flexible.
  //mixin: c'est le fait qu'un classe extend un autre classe et implemente plusieur trait au meme temps

}
